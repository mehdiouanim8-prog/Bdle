import React,{useEffect,useState}from"react";import{Platform,ScrollView,Text,View}from"react-native";import{router}from"expo-router";import{post}from"../src/api";import{getToken}from"../src/session";import{Button,Field,Step,styles,C}from"../src/ui";
export default function VerifyPhoneCode(){useEffect(()=>{if(Platform.OS==="web")router.replace("/onboarding")},[]);const[code,setCode]=useState("");const[busy,setBusy]=useState(false);const[resend,setResend]=useState(false);const[error,setError]=useState("");async function verify(){setBusy(true);setError("");try{const t=await getToken();await post("/auth/phone/verify",{code},t||undefined);router.replace("/onboarding")}catch(e:any){setError(e.message)}finally{setBusy(false)}}async function send(){setResend(true);setError("");try{const t=await getToken();await post("/auth/phone/resend",{},t||undefined)}catch(e:any){setError(e.message)}finally{setResend(false)}}return <ScrollView contentContainerStyle={styles.scroll}><View style={{flexDirection:"row",justifyContent:"space-between",marginBottom:25}}><Step n={1} label="Email" done/><Step n={2} label="Phone" active/><Step n={3} label="Profile"/></View><Text style={styles.brand}>SMS CODE</Text><Text style={styles.title}>Enter the code.</Text><Text style={styles.subtitle}>Your phone is verified only after Twilio confirms the code on the server.</Text><View style={styles.card}><Field label="SMS verification code" value={code} onChangeText={setCode} placeholder="Enter the code" keyboardType="number-pad"/><Button title="Verify phone" onPress={verify} loading={busy}/><Button title="Send a new SMS code" onPress={send} loading={resend} ghost/>{error?<Text style={styles.error}>{error}</Text>:<Text style={{color:C.muted,marginTop:14,lineHeight:20}}>Never share your verification code. Bundle staff will never ask you for it.</Text>}</View></ScrollView>}
