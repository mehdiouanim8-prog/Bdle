import React,{useEffect,useState}from"react";import{ScrollView,Text,View}from"react-native";import{router}from"expo-router";import{getToken}from"../src/session";import{api}from"../src/api";import{Button,styles,C}from"../src/ui";
export default function Review(){const[p,setP]=useState<any>();const[error,setError]=useState("");async function load(){try{const t=await getToken();setP(await api("/profile/review",{},t||undefined))}catch(e:any){setError(e.message)}}useEffect(()=>{load()},[]);return <ScrollView contentContainerStyle={styles.scroll}><Text style={styles.brand}>BUNDLE REVIEW</Text><Text style={styles.title}>Profile review.</Text><Text style={styles.subtitle}>We review profiles before discovery access. This is separate from identity verification.</Text><View style={styles.card}><Text style={{color:C.text,fontSize:22,fontWeight:"800"}}>{p?.reviewStatus||"PENDING"}</Text>{p?.reviewReason?<Text style={styles.error}>{p.reviewReason}</Text>:null}<Button title="Refresh review status" onPress={load}/>{p?.reviewStatus==="APPROVED"?<Button title="Continue to membership" onPress={()=>router.replace("/subscribe")}/>:null}{error?<Text style={styles.error}>{error}</Text>:null}</View></ScrollView>}
