import React,{useEffect,useState}from"react";import{Platform,ScrollView,Text,View}from"react-native";import{router}from"expo-router";import{put}from"../src/api";import{getToken}from"../src/session";import{Button,Field,styles}from"../src/ui";
export default function ChangePhone(){useEffect(()=>{if(Platform.OS==="web")router.replace("/onboarding")},[]);const[phone,setPhone]=useState("+");const[pw,setPw]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);async function save(){setBusy(true);setError("");try{const t=await getToken();await put("/account/phone",{phoneNumber:phone,password:pw},t||undefined);router.replace("/verify-phone-code") }catch(e:any){setError(e.message)}finally{setBusy(false)}}return <ScrollView contentContainerStyle={styles.scroll}><Text style={styles.brand}>ACCOUNT</Text><Text style={styles.title}>Change your phone.</Text><Text style={styles.subtitle}>Enter an E.164 number such as +212612345678. Bundle will send a fresh SMS verification.</Text><View style={styles.card}><Field label="New phone number" value={phone} onChangeText={setPhone} keyboardType="phone-pad"/><Field label="Current password" value={pw} onChangeText={setPw} secureTextEntry/><Button title="Send SMS verification" onPress={save} loading={busy}/>{error?<Text style={styles.error}>{error}</Text>:null}</View></ScrollView>}
