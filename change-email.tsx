import React,{useState}from"react";import{ScrollView,Text,View}from"react-native";import{router}from"expo-router";import{put}from"../src/api";import{getToken}from"../src/session";import{Button,Field,styles}from"../src/ui";
export default function ChangeEmail(){const[email,setEmail]=useState("");const[pw,setPw]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);async function save(){setBusy(true);setError("");try{const t=await getToken();await put("/account/email",{email,password:pw},t||undefined);router.replace("/verify-email")}catch(e:any){setError(e.message)}finally{setBusy(false)}}return <ScrollView contentContainerStyle={styles.scroll}><Text style={styles.brand}>ACCOUNT</Text><Text style={styles.title}>Change your email.</Text><Text style={styles.subtitle}>Your new address must be verified before it becomes trusted by Bundle.</Text><View style={styles.card}><Field label="New email" value={email} onChangeText={setEmail} keyboardType="email-address" autoCapitalize="none"/><Field label="Current password" value={pw} onChangeText={setPw} secureTextEntry/><Button title="Send verification" onPress={save} loading={busy}/>{error?<Text style={styles.error}>{error}</Text>:null}</View></ScrollView>}
