import React,{useState}from"react";import{ScrollView,Text,View}from"react-native";import{router}from"expo-router";import{put}from"../src/api";import{getToken,setSession}from"../src/session";import{Button,Field,styles}from"../src/ui";
export default function ChangePassword(){const[current,setCurrent]=useState("");const[next,setNext]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);async function save(){setBusy(true);setError("");try{const t=await getToken();const d=await put("/account/password",{currentPassword:current,newPassword:next},t||undefined);await setSession(d.token,d.refreshToken);router.back()}catch(e:any){setError(e.message)}finally{setBusy(false)}}return <ScrollView contentContainerStyle={styles.scroll}><Text style={styles.brand}>SECURITY</Text><Text style={styles.title}>Change your password.</Text><Text style={styles.subtitle}>Use a unique password for Bundle. Changing it revokes other active sessions.</Text><View style={styles.card}><Field label="Current password" value={current} onChangeText={setCurrent} secureTextEntry/><Field label="New password" value={next} onChangeText={setNext} secureTextEntry placeholder="At least 10 characters"/><Button title="Update password" onPress={save} loading={busy}/>{error?<Text style={styles.error}>{error}</Text>:null}</View></ScrollView>}
