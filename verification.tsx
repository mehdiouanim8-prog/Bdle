import React,{useState}from"react";import{Platform,ScrollView,Text,View}from"react-native";import{router}from"expo-router";import{Button,Step,styles,C}from"../src/ui";import{getToken}from"../src/session";import{post}from"../src/api";import*as WebBrowser from"expo-web-browser";import * as Linking from "expo-linking";
export default function Verification(){const[busy,setBusy]=useState(false);const[error,setError]=useState("");async function start(documentType:"GOVERNMENT_ID"|"PASSPORT"){setBusy(true);setError("");try{const t=await getToken();const d=await post("/kyc/start",{documentType},t||undefined);if(d.hostedUrl){const redirectUri=Linking.createURL("verification-complete");const result=await WebBrowser.openAuthSessionAsync(d.hostedUrl,redirectUri);if(result.type==="success")router.replace("/pending");else router.replace("/pending")}else router.replace("/pending")}catch(e:any){setError(e.message)}finally{setBusy(false)}}return <ScrollView contentContainerStyle={styles.scroll}><View style={{flexDirection:"row",justifyContent:"space-between",marginBottom:25}}>{Platform.OS==="web"?<><Step n={1} label="Email" done/><Step n={2} label="Profile" done/><Step n={3} label="Photos" done/><Step n={4} label="Identity" active/></>:<><Step n={1} label="Email" done/><Step n={2} label="Phone" done/><Step n={3} label="Identity" active/></>}</View><Text style={styles.brand}>IDENTITY VERIFICATION</Text><Text style={styles.title}>Prove it’s really you.</Text><Text style={styles.subtitle}>Bundle sends you into a specialist identity flow. You will provide a government ID or passport and complete a live liveness check. Bundle does not ask you to type document numbers into the app.</Text><View style={styles.card}><Text style={{color:C.text,fontWeight:"800",fontSize:18}}>Government ID</Text><Text style={styles.subtitle}>Use an eligible national ID or other government-issued document accepted by the provider.</Text><Button title="Verify with government ID" onPress={()=>start("GOVERNMENT_ID")} loading={busy}/></View><View style={styles.card}><Text style={{color:C.text,fontWeight:"800",fontSize:18}}>Passport</Text><Text style={styles.subtitle}>Use your valid passport and complete the same live liveness flow.</Text><Button title="Verify with passport" onPress={()=>start("PASSPORT")} ghost disabled={busy}/></View><Text style={{color:C.muted,marginTop:14,lineHeight:20}}>Liveness prompts can include moving closer, holding still, and turning your head left or right. Completion is decided by the identity provider.</Text>{error?<Text style={styles.error}>{error}</Text>:null}</ScrollView>}
