import React,{useEffect,useState}from"react";import{Platform,ScrollView,Text,View}from"react-native";import{router}from"expo-router";import{post}from"../src/api";import{getToken}from"../src/session";import{Button,Field,Step,styles}from"../src/ui";
export default function VerifyPhone(){useEffect(()=>{if(Platform.OS==="web")router.replace("/onboarding")},[]);const[phone,setPhone]=useState("+");const[busy,setBusy]=useState(false);const[error,setError]=useState("");async function send(){setBusy(true);setError("");try{const t=await getToken();await post("/auth/phone",{phoneNumber:phone},t||undefined);router.push("/verify-phone-code")}catch(e:any){setError(e.message)}finally{setBusy(false)}}return <ScrollView contentContainerStyle={styles.scroll}><View style={{flexDirection:"row",justifyContent:"space-between",marginBottom:25}}><Step n={1} label="Email" done/><Step n={2} label="Phone" active/><Step n={3} label="Profile"/></View><Text style={styles.brand}>PHONE VERIFICATION</Text><Text style={styles.title}>Verify your mobile.</Text><Text style={styles.subtitle}>Enter your number in international E.164 format. Bundle will send a one-time SMS code.</Text><View style={styles.card}><Field label="Phone number" value={phone} onChangeText={setPhone} placeholder="+212612345678" keyboardType="phone-pad"/><Button title="Send SMS code" onPress={send} loading={busy}/>{error?<Text style={styles.error}>{error}</Text>:null}</View></ScrollView>}
